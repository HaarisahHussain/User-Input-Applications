Imports System

Module Program
    Sub Main(args As String())

        'Setting syntax to accept integers only
        Dim num1 As Integer
        Dim num2 As Integer
        Dim total As Integer
        'Setiing syntax to accept String
        Dim sum As Integer
        'Getting the use to input a number 
        Console.WriteLine("Enter a number please")
        'Assigning the variable
        num1 = Console.ReadLine
        Console.WriteLine("Enter another number please")
        num2 = Console.ReadLine
        'Addition
        total = num1 + num2
        Console.WriteLine("This would make" & "  " & total)
        'Outputs the total of num1 and num2
        sum = Console.ReadLine()
        Console.ReadLine()






    End Sub
End Module
